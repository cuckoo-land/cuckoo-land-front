import Table from '@components/gameroom/table';
import Layout from '@components/layout';

function GameRoom() {
  return (
    <Layout title="Gameroom" seoTitle="Gameroom">
      <div>
        <Table />
      </div>
    </Layout>
  );
}

export default GameRoom;
