export const idRegExp = /^[a-zA-Z0-9]{4,16}$/;
export const nicknameRegExp = /^[가-힣a-zA-Z0-9]{2,16}$/;
export const passwordRegExp = /^(?=.*[a-z])(?=.*[0-9])[0-9A-Za-z$&+,:;=?@#|'<>.^*()%!-]{8,32}$/;
